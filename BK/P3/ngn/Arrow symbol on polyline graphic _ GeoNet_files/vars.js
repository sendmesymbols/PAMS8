define({
    videos:              {}, //holds all the videos we have on the page
    enableAutoResize:    true, //do we want to resize the player
    fixedPlayerSize:     false, //locks the player sizes to a fixed width and height
    fixedWidth:          undefined,
    fixedHeight:         undefined,
    lastAutoplay:        false,
    lastWatermark:       false
});
