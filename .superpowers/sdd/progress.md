# Progress ledger — Curved Arrows (2026-07-25-curved-arrows.md)

Execution mode: directly on `master`, no worktree, no commits (user's explicit
choice — see conversation). Per-task diffs are snapshot-based
(`git diff --no-index` between a pre-task copy and the post-task file), not
commit-range diffs. Nothing is committed at any point; the working tree
carries all changes cumulatively across tasks.

Tasks:
- Task 1: complete (arrowType field, BriefingTypes.ts) — review clean, no commits (working tree only)
- Task 2: complete (sharp/curved/elbow path builders + buildArrowPath, OverlayFabric.ts) — review clean, no commits
- Task 3: complete (N-point makeArrowGroup + round-trips, OverlayFabric.ts; drag-arrow call site adapted, SlideEditor.ts) — review clean. Implementer found + fixed a necessary out-of-brief consequence: 5 sites in SlideEditor.ts (_applyStyleTo x3, _syncControlsFromSelection x2, actually 4+1) that located an arrow's stroke child via `ch.type === 'line'` now check `ch.type === 'path'` (arrow body child changed from fabric.Line to fabric.Path). Verified complete (no 6th site) and correctly scoped to kind==='arrow' only. No commits.
- Task 4: complete (click-chain multi-point Arrow tool, replacing drag; StyleDefaults.arrowType) — review clean/Approved. Deviation from brief (correct, verified): `type ArrowType` import NOT added to SlideEditor.ts (genuinely unused there under this project's real noUnusedLocals:true) — Task 5 must add it when it first needs the explicit type annotation. Minor findings deferred to final review: (a) SlideEditorUI.ts:113 TOOL_DEFS arrow tooltip still says "Arrow (drag)", now stale; (b) _arrowChain/_arrowPreview lack explicit reset in close()/_loadSlide() (harmless today per reviewer's trace, but asymmetric with _drawing/_lassoPts/_erasing). No commits.
- Task 5: complete (bow-handle segment editing via fabric.Control) — Approved after 3 fix rounds:
  (1) Critical bug found by implementer (verified against real fabric.js 4.5.0 source): brief's per-tick destroy/recreate design would orphan duplicate arrow objects on every drag since fabric pins transform.target for the whole gesture. Redesigned by controller: non-destructive per-tick preview object + one-shot real rebuild on `object:modified`. Verified via multi-tick retrace.
  (2) Important finding from review: mid-drag Delete/Undo could resurrect/duplicate the arrow. Fixed with defensive _bendDrag/_bendPreview clears in _deleteSelection/_restore.
  (3) Reviewer found an undisclosed `styleSelectionControls()` addition (global fabric.Object.prototype restyle) not requested anywhere in this plan; its safety comment falsely claimed the Briefing editor's canvas is the only fabric.Canvas in the app (index.html's #fabricCanvas contradicts this). Reverted, then user confirmed it was their own intentional addition (from a concurrent session — see docs/superpowers/specs/2026-07-25-selection-handle-style-design.md, same-day sibling spec, independently specifying the identical paired restore-on-close design) and asked to keep it. Re-added, scoped: snapshots prototype state on open, restores on close, so the global mutation no longer persists past the editor's own lifetime.
  Also accepted (necessary, disclosed, verified complete): _addOverlays (paste/duplicate) and _restore (undo/redo) gained _attachArrowControls calls — gaps nothing else in the 7-task plan covered.
  No commits.
- Task 6: complete (Sharp/Curved/Elbow panel selector, SlideEditorUI.ts + SlideEditor.ts _onStyleChanged/_applyArrowTypeChange) — Approved. Implementer's self-flagged multi-select-rebuild-ordering concern was investigated by reviewer against real fabric.js 4.5.0 internals and confirmed NOT a real risk (canvas-level remove/add never touches ActiveSelection's own cached transform). One Minor note deferred to final review: arrowType branch doesn't dissolve the ActiveSelection via the existing _withFlatSelection pattern before rebuilding members (not a bug today, latent fragility only if _rebuildArrow ever changes to group-aware removal). No commits.
- Task 7: complete (reopen an existing arrow via Arrow tool while selected, append points, Escape restores original) — Approved. Implementer found + fixed a real data-loss bug beyond the brief: _saveCurrent() read the canvas directly, so navigating/closing-with-save/presenting mid-reopen (without Escape/finish) would silently drop the arrow being edited from the saved slide. Controller identified a 3rd affected call site (_onAction's 'present' case) the implementer's initial report missed; fixed centrally inside _saveCurrent() itself (calls _clearArrowChain() when _arrowChain is set, before reading getObjects()) so all 3 current call sites + any future ones are covered. Verified by reviewer via full-file grep (exactly 3 call sites, none missed). Minor findings deferred to final review: (a) _defaults not restored to pre-reopen values on Escape-cancel of a reopen (only affects next newly-drawn shape's style); (b) one redundant _syncPanelContext repaint per reopen (cosmetic). No commits.

All 7 implementation tasks complete. Proceeding to final whole-branch review + manual browser verification.

## Final whole-branch review (opus) + fix pass

Found 2 Critical, 4 Important, ~9 Minor issues invisible to individual task reviews (cross-task interactions):
- CRITICAL: close()/_loadSlide() never reset _arrowChain/_arrowPreview/_arrowReopenedObj/_bendDrag/_bendPreview -> Cancel button during a reopen leaks state -> next editor open resurrects a stale duplicate arrow with a duplicate overlay id.
- CRITICAL: _restore() (undo/redo) didn't clear the reopen state either -> undo during a reopen could resurrect/duplicate the arrow or silently defeat the undo.
- IMPORTANT: _rebuildArrow / _onArrowReopen didn't account for the arrow group's scale -> switching type or bow-handle-dragging a SCALED arrow visibly shrank/grew its stroke+arrowhead.
- IMPORTANT: _rebuildArrow always remove+add (no z-order preservation) -> switching an arrow's type or inserting a bend silently sent it to the front, undoing manual layering.
- IMPORTANT: Elbow arrows incorrectly got bow handles (spec says they shouldn't) and the handle floated off the actual rendered (orthogonal) path.
- IMPORTANT (descoped, needs a decision, NOT fixed): PptxExporter.ts ignores arrowType on export -- curved exports as sharp (acceptable), elbow exports as a bare diagonal (wrong shape). Out of this plan's declared file scope (BriefingTypes/OverlayFabric/SlideEditor/SlideEditorUI only); flagged to user as a known limitation / follow-up decision.
- Minor (fixed): stale "Arrow (drag)" tooltip; stale "Reused by Task 6" doc-comment artifact.
- Minor (accepted, not fixed): multi-arrow type-change pushes one undo entry per arrow; a 107-char line; arrowSharp icon duplicates the existing arrow icon; ELBOW_FILLET_PX is an unscaled fixed pixel constant.

All Critical/Important items within plan scope were fixed in one pass and independently re-reviewed: Approved, no further issues found.

STATUS: 7/7 tasks complete, final review clean. Remaining open item: PPTX export for curved/elbow arrowType (user decision needed, not part of this plan's file scope). No commits made at any point in this entire implementation -- all changes live directly in the working tree on master, per user's explicit standing preference.

---

# Progress ledger — Briefing Review Comments (2026-07-26-briefing-comments.md)

Execution mode: directly on `master`, no worktree, **no commits** (user's standing
preference, same as the curved-arrows run above). Per-task diffs are
snapshot-based (`git diff --no-index` between .superpowers/sdd/snap-N/ copies and
the post-task files), not commit ranges. The working tree carries all changes
cumulatively across tasks.

Verification mode: user chose ONE manual pass at the end. Implementers gate on
code + type-check + node unit tests only; all 24 GUI checks (Tasks 2,3,4) and the
PowerPoint check (Task 6) are deferred to a single manual pass after Task 6.

Tasks:
- Task 1: complete (SlideComment/SlideCommentEntry/Slide.comments + version 7 in BriefingTypes.ts; new SlideCommentUtils.ts with commentUuid/projectAnchor/relTime/pruneComments/threadCount/openCount; new SlideCommentUtils.test.ts 21/21 passing) — review Approved, no Critical/Important. Reviewer independently re-ran the test + type-check and confirmed the TDD evidence is genuine; also checked the version union 6->7 breaks no exhaustive switch anywhere in MS/. Controller resolved the reviewer's one ⚠️ (could another in-flight modified file assume a different SlideComment shape?): no — grep at session start showed zero comment references in Briefing/ or ImportExport/ apart from an unrelated `staffComments` amplifier field. Minor findings deferred to final review: (a) pruneComments' copy is shallow so a returned thread shares its `replies` array reference with the input (harmless today; Task 2's _reply builds a new array rather than mutating); (b) the "uuids differ" assertion is probabilistic rather than logical; (c) implementer's report misstated its own file line counts. No commits.
- Task 2: complete (new SlideComments.ts = CommentsHost + CommentsLayer with mount/unmount/load/refresh/addComment/openComposer/openThread/closePopover; .ms-sledit-cmt* CSS in SlideEditorUI.ts; SlideEditor.ts owns _comments working array + _commentsHost() + 7 refresh call sites + save patch + teardown; BriefingEngine.ts persists comments and bumps exportBriefing to version 7) — Approved after 2 fix passes.
    (1) Controller-initiated fix: implementer left 3 premature symbols that no Task 2 code used and which are hard noUnusedLocals errors — `openCount` imported into SlideEditor.ts (a plan error: nothing in any task uses it there; the rail/strip badges that need it live in BriefingEngine.ts), `SlideComment` imported into BriefingEngine.ts, and `BACKDROP_AREA` made `export` purely to dodge an unused-const error. All three removed under the rule established in the curved-arrows run: a symbol enters the code in the task that first uses it.
    (2) Reviewer found an Important lifecycle bug: the thread popover (body-mounted) was only torn down by unmount(), which runs from close() or the next mount() — and mount() happens inside _initCanvas, AFTER _loadSlide's awaits. Arrow-key slide nav fires keydown not pointerdown, so the popover's outside-click dismiss never fired either. Result: popover stayed interactive across a slide switch while _comments had already been swapped, so Reply/Resolve/Delete silently no-op'd (id matched nothing; map/filter returned unchanged copies) and the user saw it close as if saved. Fixed per the file's own convention: _closePopover() → public closePopover() (8 internal call sites renamed), called at SlideEditor.ts:487 beside the existing ui.hideContextMenu()/ui.closeHelp() pair at the top of _loadSlide. Reviewer independently re-read the live files, re-enumerated all 8 renames, and confirmed the call lands before both the _comments reassignment and the first await.
    Controller resolved the reviewer's ⚠️ on GUI check 4: _resizeStageToFit early-returns only when the fit is UNCHANGED, in which case markers need no repositioning, so placing refresh() after the early return is correct.
    CSS tokens needed no substitution — the repo already defines --sl-surface/-line/-text/-dim/-input/-accent (SlideEditorUI.ts:2266-2273).
    Minor findings deferred to final review: (a) refresh() does a full innerHTML teardown/rebuild plus sync layout reads on every object:moving/object:scaling/pan tick, where an in-place position update would do; (b) _markerFor uses positional `_layer.children[index]` lookup, coupling DOM order to array order with no data-comment-id fallback; (c) slide-level marker stacking is uncapped, so many slide-level threads would run off the canvas bottom. No commits.
- Task 3: complete (CommentsLayer.arm/disarm/armed/onArmChange/anchorAt + 3-state hover preview in SlideComments.ts; hover-preview CSS + 💬 topbar button + setCommentMode + help row in SlideEditorUI.ts; _toggleCommentMode + N key + Ctrl+Alt+M + Escape rung + _setTool disarm in SlideEditor.ts) — Approved after 1 fix pass.
    Controller pre-corrected two stale brief references before dispatch (the brief predated Tasks 1-2): BACKDROP_AREA had been removed in Task 2's fix pass so Task 3 reintroduces it as a private non-exported const (0.8); and `this._closePopover()` is now `this.closePopover()`.
    Implementer found the brief's suggested Ctrl+Alt+M placement was DEAD CODE — the pre-existing `mod && e.altKey` branch begins with `if (k !== 'c' && k !== 'v') return;`, which would have swallowed 'm' first. Moved the check before that guard, still inside the same branch. Reviewer independently read the control flow and confirmed both the dead-code diagnosis and that the new placement is the only one that fires.
    Reviewer verified analytically (not by trusting the report) that anchorAt is the exact mathematical inverse of projectAnchor, so the hover pin and the click anchor cannot disagree; also confirmed the hit-test's getBoundingRect(false,false) base space and the preview's _objectBox getBoundingRect(true,true) screen space never need to agree with each other directly.
    Fix pass (reviewer's Important finding): arm() attaches two capture-phase document listeners, but close() called unmount() which never cleared _armCleanup — and arm()'s onDown deliberately ignores clicks inside .ms-sledit-topbar so the 💬 toggle works, which also shields Save & Close / Cancel from the cleanup branch. Result: closing the editor while armed leaked both listeners holding a closure over the discarded layer and removed DOM (self-healing on the next mousedown anywhere, but live until then). Fixed in ONE place: unmount() now calls disarm() first, so every caller is covered (mirrors mount() calling unmount()); no disarm added to close().
    Fixer then surfaced a second window of the same class, which the controller judged in-scope as implementing the plan's stated intent rather than changing design: _setTool('select') does disarm, and _loadSlide calls it — but only at the END, after the prepareBackground/_loadImage/preloadOverlayImages awaits, so the entire async load window stayed armed against the outgoing slide's layer. Fixed with this._cmt?.disarm() at SlideEditor.ts:489, beside the Task 2 closePopover() call at the top of _loadSlide. Reviewer confirmed disarm() is idempotent (cleanup() nulls _armCleanup first, and disarm() is `this._armCleanup?.()`), so the resulting three disarm calls per load are all safe; also confirmed mount() is reachable only from _initCanvas which is reachable only from _loadSlide, so the transitive disarm never surprise-fires mid-slide (_resizeStageToFit resizes in place and never calls _initCanvas).
    Minor findings deferred to final review: (a) anchorAt's pure inverse-transform arithmetic is buried in the DOM class rather than extracted as an `unprojectAnchor` counterpart in the node-testable SlideCommentUtils.ts, so a future change to projectAnchor's formula could silently desync from its inverse with no test to catch it (brief-mandated code, math verified correct today); (b) the topbar exemption also leaves comment mode visibly armed when the notes drawer or help overlay is opened from the topbar — cosmetic, not a leak. No commits.

WORKSPACE NOTE (mid-run correction): this run's artifacts were initially written to
the same .superpowers/sdd/task-N-brief.md / task-N-report.md paths the completed
curved-arrows run had used, so Tasks 1-5 of this run overwrote that run's briefs and
reports. Caught after a security warning on Task 5's report write. All of this run's
artifacts were moved into .superpowers/sdd/briefing-comments/ (matching the existing
slide-transitions/ convention) and every curved-arrows file was restored with
`git checkout HEAD --` (they were tracked, so nothing was permanently lost). Only
progress.md remains modified, because this section is an append, not an overwrite.
Task 6 onward uses the namespaced paths.
- Task 4: complete (rail thumb badge + RailHost.slides() widened with openComments; Comments review section with All-slides scope in SlideEditorUI.ts; EditorUIHost.comments/allComments/goToComment + _goToComment in SlideEditor.ts; briefing strip badge + host listComments + public BriefingEngine.listComments() in BriefingEngine.ts) — Approved after 2 fix passes.
    Reviewer verified both listComments shapes stay distinct as designed (host: Array<{slideIndex, comment}> for the All-slides scope; public: flattened typed-anchor rows for scripting), that all three user-input interpolations in refreshComments route through _escape, and that no symbol was imported ahead of its use.
    Fix pass 1, two Important findings:
      (a) Badge liveness — refreshRail() sourced counts from BriefingEngine's PERSISTED _slides[i].comments (written only by onSaved), so resolving a thread on the open slide left its own badge unchanged until save/navigate, failing the brief's check 5. Fixed using this task's own new EditorUIHost.comments() accessor: refreshRail() takes the current index's count from openCount(this._host.comments()) (the live working array) and every other index from the persisted list. Ownership model untouched — nothing in the UI layer writes to BriefingEngine._slides and setComments still does not save. Reviewer confirmed the briefing STRIP badge has no equivalent staleness (the editor is a fully opaque position:fixed z-index:9700 overlay and every BriefingEngine overlay is <=9600, so the strip is provably never visible while the editor is open; and _refreshStrip only runs from onSaved after s.comments is already fresh) — so the strip was correctly left alone.
      (b) Badge/button CSS collision (plan-mandated coordinates) — .ms-sledit-thumbcmt at top:4/right:4 landed exactly on .ms-sledit-thumbtools (hover duplicate+delete, ~40px) and .ms-brief-cmt at top:3/right:3 on .ms-briefing-tile-del/-edit, painting above them with a positive z-index and no pointer-events opt-out, so a slide with open comments could lose click access to delete. Judged in-scope to fix (the plan's REQUIREMENT is a visible unresolved-count badge; the corner was incidental detail): rail badge moved to bottom:4/left:4 (verified unclaimed), strip badge to top:3/left:26 (clears .ms-briefing-tile-num's 11px bold glyph span; bottom is the title bar, top-right the hover buttons), and pointer-events:none added to BOTH so neither can ever intercept a click again.
    Fix pass 2 — the liveness fix introduced a WORSE regression the reviewer caught: ui.updateNav() (which calls refreshRail()) ran at line 495 while `this._comments` was still reassigned down at line 512, so the override read the PREVIOUS slide's array (or [] after close()/on first open). Net effect: the open slide's badge was absent on every open and every navigate until a comment edit or a manual rail-resizer drag — more frequently triggered than the original bug, and failing check 5 in the brief's own prescribed setup. Fixed by moving the assignment to line 488, immediately after `this._index = index` and ahead of updateNav. Reviewer then confirmed: exactly one _comments assignment inside _loadSlide; the six intervening calls don't read it; _buildStage's earlier build()->refreshRail() stale frame is never painted (open() calls _buildStage then _loadSlide with no intervening await, and _loadSlide reaches the corrected updateNav before its first await); and the reorder adds no mid-load save risk, since _saveCurrent's pre-existing `!this._fc` guard already blocks saves for the whole in-flight window.
    Minor findings deferred to final review: (a) the overlayId -> x/y -> slide-level anchor precedence is reimplemented in three places (refreshComments' `where`, BriefingEngine.listComments' anchor chain, CommentsLayer.openThread's label) rather than shared as an anchorKind() helper in the node-testable utils — two of the three mirror brief-supplied code; (b) _goToComment silently no-ops on a stale slideIndex, leaving a dangling pendingThread that the next successful load drops, with no user feedback. No commits.
- Task 5: complete (new MS/Engines/ImportExport/PptxComments.ts = pure legacy-PowerPoint comment-part builders: EIGHTH_POINTS_PER_INCH/REL_TYPE_*/CT_* constants, PptxCommentRecord, PptxCommentParts, buildCommentParts, addRelationship, addContentTypeOverrides; new PptxComments.test.ts) — Approved after 1 fix pass. 27/27 assertions passing (22 originally, +5 in the fix pass; the brief's expected 22 was correct and the implementer was told to report the real count rather than pad to it).
    Reviewer hand-traced every load-bearing algorithm: authorId 1..n in first-seen order with clrIdx=id-1 and lastIdx=that author's highest idx; idx per-AUTHOR across the document rather than per-slide; part paths numbered over COMMENTED SLIDES in ascending slide order and independent of slide numbers (slide-3-only -> comment1.xml); esc() applied to author, initials, text and rel target in the correct &-first order; addRelationship recomputing max+1 from the passed rels content per call; addContentTypeOverrides' quote-delimited guard correctly avoiding a comment1.xml/comment10.xml substring false positive. Also confirmed zero imports of any kind in PptxComments.ts (so the bare-node test runs), and no Task 6 symbol declared early.
    THE UNITS FACT held: EIGHTH_POINTS_PER_INCH = 576 (1/8 pt = 1/576 in), asserted against a hardcoded 576 so a "correction" to an EMU value would fail immediately. This is the single thing that decides whether comments land where the editor markers were rather than all in the slide's top-left corner — MS-OI29500 §19.4.5 note (b) says PowerPoint reads p:pos as ST_EighthPointMeasure even though ISO/IEC 29500 declares it ST_Coordinate (EMU).
    Fix pass (two Important findings, both plan-mandated gaps in the brief's own test file, judged purely additive to fix since adding assertions contradicts no plan requirement): (a) dtOf's unparseable-date fallback was untested — now asserts dt="1970-01-01T00:00:00.000" from at:'not-a-date' plus that the output never contains the literal "Invalid Date"; (b) REL_TYPE_COMMENT_AUTHORS, CT_COMMENTS and CT_COMMENT_AUTHORS were imported nowhere and never asserted, so a single typo in an OOXML URI or MIME string would silently produce comment parts PowerPoint refuses to open — each now compared against a hand-written literal. Reviewer verified the literals are written out in the test rather than derived from the module (ruling out the compare-a-constant-to-itself failure mode), diffed all three character-for-character against PptxComments.ts:30-35, and re-ran the file to see 27/0 itself.
    Minor findings deferred to final review: (a) REL_TYPE_COMMENTS is still never asserted against its own literal — the addRelationship tests only check id-allocation mechanics (rId7/rId8), not that the emitted Type="..." equals the constant; now asymmetric with the other three, one line to close; (b) no fractional/negative/NaN coordinate coverage, and a NaN x/y would literally emit x="NaN" (unvalidated, unclamped); (c) two authors sharing an identical name collapse into one PowerPoint author, inherent to PptxCommentRecord carrying only a name string; (d) authorId first-seen order is keyed off raw input-array order while idx/part-numbering use slide-sorted order, and the test data can't disambiguate the two readings; (e) redundant `w[0]!` non-null assertion (noUncheckedIndexedAccess is off); (f) implementer's report misstated its own per-section assertion counts and file line counts. No commits.
- Task 6: complete (PPTX_MIME + injectPptxComments JSZip glue in PptxComments.ts; _collectComments + pptx-slide counter + blob download replacing pptx.writeFile in PptxExporter.ts) — Approved after 1 fix pass. Tests 33/33 + 21/21, Vite build succeeds.
    Implementer resolved a delegated ambiguity correctly: the real export loop has FOUR _addSlide sites, not the brief's "three branches" — screen-only, the explodeBuilds reveal loop, the plain branch, and a no-briefing single-view fallback. pptxSlideNo++ went beside emitted++ at all four; _collectComments is called only at the three that have a real BriefingSlide. Reviewer read the full exportDeck() body and confirmed the counter cannot desync: _addSlide calls pptx.addSlide() unconditionally as its first statement, and the reveal===0 guard skips only the collection, never the increment. This mattered because injectPptxComments uses that number to find ppt/slides/_rels/slideN.xml.rels — a drifting counter would attach comments to the wrong slides.
    Reviewer also confirmed the comment-part numbering vs rels-path distinction is right (commentN.xml counts only commented slides, but the .rels file targeted is keyed off the real slide number).
    Fix pass, two Important findings, both plan-mandated, both judged worth fixing (neither contradicts a plan REQUIREMENT — the plan requires comments to be emitted, not a particular error-handling posture or file placement):
      (a) No injection fallback: injectPptxComments throws hard on a missing rels part or [Content_Types].xml, and a slide whose screenshot failed can plausibly have no rels part — so one comment on such a slide would lose the ENTIRE deck (every slide, every screenshot already computed) over a best-effort feature, surfaced only as a console error, and inconsistent with the file's own warn-and-continue style everywhere else. Fixed in the CALLER (the injector correctly still throws): try/catch around only the injection call, EngineLogger.error on failure, fall back to the comment-free blob so the user still gets their .pptx. Reviewer confirmed pptx.write stays OUTSIDE the try so a real write failure still propagates rather than being swallowed.
      (b) No automated coverage for the eighth-point arithmetic — the single highest-risk formula in the feature, verified only by a manual PowerPoint check that hadn't run, because it lived on a class that can't be node-imported (@arcgis transitive imports). Extracted as pure `commentAnchorEighths(fit, overlayBox, point, stackIndex): {x,y}` in PptxComments.ts (restoring the pure/glue split the plan's own §5 establishes) and covered by 6 new assertions, including one pinning the conversion itself (2in x 576 = 1152, 3in x 576 = 1728) so a regression to EMU would yield 1828800 and to points 144 — either fails loudly.
      Reviewer did NOT take the extraction on trust: it compared the new function against the inline code it had itself quoted in its first pass, then EXECUTED the new function and matched all five sample outputs (overlay {3456,324}, point {2880,1620}, fallback stack 0/1/2) against its own hand-derivation from the pre-fix formula. Also grepped the whole file to confirm zero imports and the only globalThis reference still function-scoped, so node-importability is intact.
    Minor findings deferred to final review: (a) NEW — the EngineLogger.success line appends "N comment entries" whenever commentRecords.length is truthy without checking whether injection actually succeeded, so a caught fallback prints a contradictory error+success pair in the log (the shipped file is correctly comment-free; only the diagnostic is wrong); (b) the (n) => { skippedResolved += n } closure is duplicated verbatim at all three collect sites; (c) _collectComments/_downloadBlob add ~50 lines to an already 1500+-line PptxExporter.ts.

ALL 6 IMPLEMENTATION TASKS COMPLETE. Proceeding to final whole-branch review, then handing the deferred manual verification (24 GUI checks across Tasks 2-4, plus the zip-structure inspection and PowerPoint open for Task 6) to the user.
